1. possibly implement a state machine for flight status
    Ex:
        SCHEDULED → BOARDING → DEPARTED → ARRIVED
        SCHEDULED → DELAYED → SCHEDULED
        SCHEDULED → CANCELLED
        BOARDING → CANCELLED (rare but possible)
2. Use jOOQ for dynamic filtering for fetching records (e.g fetching flights filtered by origin/date/destination)